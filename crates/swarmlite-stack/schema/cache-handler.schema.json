{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/gfreezy/swarmlite/main/crates/swarmlite-stack/schema/cache-handler.schema.json",
  "title": "Swarmlite Gateway response cache",
  "description": "Route-level settings for Swarmlite's native Caddy HTTP cache handler and managed SQLite response store.",
  "type": "object",
  "properties": {
    "ttl": {
      "$ref": "#/$defs/duration",
      "default": "5m",
      "description": "How long a stored response remains fresh."
    },
    "allowed_http_verbs": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "pattern": "^[!#$%&'*+.^_`|~0-9A-Za-z-]+$",
        "not": { "enum": ["CONNECT", "connect"] }
      },
      "default": ["GET", "HEAD"],
      "description": "Souin-compatible method allowlist. When omitted, only GET responses are stored and HEAD may reuse a matching GET response; CONNECT and protocol upgrades always bypass the cache."
    },
    "max_cacheable_body_bytes": {
      "type": "integer",
      "minimum": 1,
      "default": 10485760,
      "description": "Maximum response body captured for one cache entry."
    },
    "max_request_body_bytes": {
      "type": "integer",
      "minimum": 1,
      "default": 1048576,
      "description": "Maximum request body hashed into a cache key; larger requests bypass the cache."
    },
    "key": {
      "$ref": "#/$defs/key",
      "description": "Souin-compatible cache key settings. Native keys are always hashed, so hash is accepted without changing key identity."
    },
    "status_codes": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "integer",
        "minimum": 200,
        "maximum": 599,
        "not": { "const": 304 }
      },
      "default": [200],
      "description": "Response status codes eligible for storage."
    }
  },
  "additionalProperties": false,
  "$defs": {
    "duration": {
      "type": "string",
      "minLength": 1,
      "description": "A duration such as 500ms, 60s, or 5m."
    },
    "key": {
      "type": "object",
      "properties": {
        "headers": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[!#$%&'*+.^_`|~0-9A-Za-z-]+$"
          }
        },
        "disable_query": { "type": "boolean" },
        "hash": { "type": "boolean" }
      },
      "additionalProperties": false
    }
  }
}
