[package]
name = "swarmlite-gateway"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Swarmlite HTTP routing schema, validation, and Caddy JSON renderer"

[dependencies]
anyhow = "1.0"
regex = "1.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

